version: '3.8'
services:
  db:
    image: postgres:15
    container_name: bank_rest_db
    environment:
      - POSTGRES_DB=bank_db
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
